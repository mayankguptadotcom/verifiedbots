{
  "name": "verifiedbots-data",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "validate": "tsx src/cli/validate.ts",
    "fetch-feeds": "tsx src/cli/fetch-feeds.ts",
    "verify-wba": "tsx src/cli/verify-wba.ts",
    "build:artifacts": "tsx src/cli/build.ts",
    "build:site": "npm --prefix site run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.1",
    "ipaddr.js": "^2.2.0",
    "yaml": "^2.6.0"
  }
}
